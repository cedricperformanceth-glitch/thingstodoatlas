---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CityGuidePage from '../../components/CityGuidePage.astro';
import { getCityGuide } from '../../data/contentModel';

const city = getCityGuide('sri-lanka', 'negombo');
if (!city) throw new Error('Negombo city guide is not configured');
---

<BaseLayout
  title="Things to do in Negombo, Sri Lanka"
  description={city.description}
  canonical="https://thingstodoatlas.com/sri-lanka/negombo"
>
  <CityGuidePage city={city} />
</BaseLayout>
