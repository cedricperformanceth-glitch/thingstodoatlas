---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CityGuidePage from '../../components/CityGuidePage.astro';
import { getCityGuide } from '../../data/contentModel';

const city = getCityGuide('sri-lanka', 'mirissa');
if (!city) throw new Error('Mirissa city guide is not configured');
---

<BaseLayout
  title="Things to do in Mirissa, Sri Lanka"
  description={city.description}
  canonical="https://thingstodoatlas.com/sri-lanka/mirissa"
>
  <CityGuidePage city={city} />
</BaseLayout>
