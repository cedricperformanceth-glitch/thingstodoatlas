---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CityGuidePage from '../../components/CityGuidePage.astro';
import { getCityGuide } from '../../data/contentModel';

const city = getCityGuide('sri-lanka', 'colombo');
if (!city) throw new Error('Colombo city guide is not configured');
---

<BaseLayout
  title="Things to do in Colombo, Sri Lanka"
  description={city.description}
  canonical="https://thingstodoatlas.com/sri-lanka/colombo"
>
  <CityGuidePage city={city} />
</BaseLayout>
