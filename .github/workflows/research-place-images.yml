name: Sync place images

on:
  workflow_dispatch:
    inputs:
      refresh_generic:
        description: Retry exact research for records currently using a generic fallback
        required: false
        default: false
        type: boolean
  push:
    branches:
      - master
    paths:
      - 'src/data/**/*.ts'
      - 'src/data/**/*.tsx'
      - 'src/pages/**/*.astro'
      - 'src/pages/**/*.ts'
      - 'src/pages/**/*.tsx'
      - 'src/data/**/*-image-candidates.json'
      - 'scripts/find-place-images.mjs'
      - 'scripts/place-data-discovery.mjs'
      - 'scripts/sync-place-images.mjs'
      - 'scripts/audit-place-image-system.mjs'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/research-place-images.yml'

permissions:
  contents: write

concurrency:
  group: all-place-image-research
  cancel-in-progress: false

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Synchronise images for every standard city and category
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ inputs.refresh_generic }}" == "true" ]]; then
            pnpm run images:sync -- --refresh-generic
          else
            pnpm run images:sync
          fi

      - name: Audit city and category coverage
        run: pnpm run images:audit

      - name: Validate production build
        run: pnpm run build

      - name: Commit image research results
        shell: bash
        run: |
          if git diff --quiet -- src/data/place-images.generated.json; then
            echo "No place image results changed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/data/place-images.generated.json
          git commit -m "Sync place images for all active cities"
          git push
